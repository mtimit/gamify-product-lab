# 🔧 Инструкция по отладке

## Если вкладки не кликабельны:

### Шаг 1: Откройте index.html в браузере
Используйте один из способов:
- **Правый клик на index.html** → "Open with Live Server" (если установлено расширение)
- Или просто откройте файл двойным кликом

### Шаг 2: Откройте консоль разработчика
- Нажмите **F12** (или Ctrl+Shift+I)
- Перейдите на вкладку **Console**

### Шаг 3: Проверьте логи
Вы должны увидеть:
```
Инициализация приложения...
State загружен: {version: "1.0", gameProfile: {...}, ...}
Найдено кнопок табов: 3
Настройка таба: dashboard
Настройка таба: projects
Настройка таба: experiments
Настройка обработчиков событий...
Форма проектов найдена
Приложение готово к работе!
```

### Шаг 4: Попробуйте кликнуть на вкладки
- Кликните на **Projects** или **Experiments**
- В консоли должно появиться: `Клик по табу: projects`

### Шаг 5: Проверьте возможные ошибки

#### ❌ Если видите ошибку "Failed to load module script"
**Проблема:** Браузер блокирует загрузку модулей из-за CORS политики

**Решение:** Запустите локальный сервер:

**Для Windows PowerShell:**
```powershell
cd C:\Users\hollo\Documents\gamify-product-lab
python -m http.server 8000
```

Затем откройте: http://localhost:8000

**Альтернатива - используйте Live Server:**
1. Установите расширение "Live Server" в VS Code
2. Правый клик на index.html
3. Выберите "Open with Live Server"

#### ❌ Если ничего не появляется в консоли
**Проблема:** JavaScript не загружается

**Проверьте:**
1. Путь к скрипту в index.html: `<script type="module" src="./src/app.js"></script>`
2. Файл src/app.js существует
3. Нет синтаксических ошибок (смотрите красные ошибки в консоли)

#### ❌ Если табы не переключаются
**Проблема:** CSS или JavaScript конфликт

**Проверьте в консоли:**
- Есть ли сообщения "Клик по табу: ..."
- Откройте вкладку **Elements** (F12)
- Найдите элемент с классом `.screen.active`
- Убедитесь что у него style `display: block`

## Быстрый тест

Откройте **test.html** в браузере - он проверит базовую функциональность.

## Проверка вручную

Откройте консоль (F12) и выполните:

```javascript
// Проверка загрузки
console.log("State:", JSON.parse(localStorage.getItem('gamify-product-lab-state')));

// Проверка табов
document.querySelectorAll('.tab-button').forEach(btn => {
  console.log('Tab button:', btn.dataset.screen, btn);
});

// Ручное переключение таба
document.querySelector('[data-screen="projects"]').click();
```

## Всё ещё не работает?

1. **Очистите кэш браузера:** Ctrl+Shift+Delete
2. **Попробуйте другой браузер:** Chrome, Firefox, Edge
3. **Проверьте расширения браузера:** Отключите AdBlock и подобные
4. **Проверьте права доступа:** Убедитесь что браузер может читать файлы из папки проекта
